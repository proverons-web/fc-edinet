import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import type { MediaAlbum, MediaVideo } from "@/lib/types";
import { getLocale } from "@/lib/locale";
import { dateLocale, publicText, type Locale } from "@/lib/i18n";
export const dynamic="force-dynamic";
export default async function MediaPage(){const locale=await getLocale();const text=publicText[locale].media;const supabase=await createClient();const [{data:albumsData},{data:videosData}]=await Promise.all([supabase.from("media_albums").select("*").eq("is_published",true).order("display_order").order("event_date",{ascending:false}).order("created_at",{ascending:false}),supabase.from("media_videos").select("*").eq("is_published",true).order("display_order").order("published_at",{ascending:false}).limit(6)]);const albums=(albumsData??[]) as MediaAlbum[];const videos=(videosData??[]) as MediaVideo[];return <main><section className="mediaHero"><div className="container"><p className="eyebrow">FC EDINEȚ MEDIA</p><h1>{text.heroTitle}</h1><p>{text.heroText}</p></div></section><section className="section mediaAlbumsSection"><div className="container"><div className="sectionHeading"><div><p className="eyebrow blue">{text.photo}</p><h2>{text.albums}</h2></div></div>{albums.length?<div className="mediaAlbumGrid">{albums.map((album,index)=><Link href={`/media/${album.slug}`} className={`mediaAlbumCard ${index===0?"featured":""}`} key={album.id}><div className="mediaAlbumCover">{album.cover_image_url?<img src={album.cover_image_url} alt={album.title}/>:<span>FC EDINEȚ</span>}<div className="mediaAlbumOverlay"/><div className="mediaAlbumCardContent"><span>{formatDate(album.event_date,locale)}{album.location?` · ${album.location}`:""}</span><h3>{album.title}</h3>{album.description&&<p>{album.description}</p>}<b>{text.openAlbum}</b></div></div></Link>)}</div>:<div className="adminEmpty">{text.albumsEmpty}</div>}</div></section><section className="section mediaVideosSection"><div className="container"><div className="sectionHeading light"><div><p className="eyebrow">FC EDINEȚ TV</p><h2>{text.videos}</h2></div></div>{videos.length?<div className="mediaVideoGrid">{videos.map(video=><article className="mediaVideoCard" key={video.id}><div className="mediaVideoEmbed"><iframe src={`https://www.youtube-nocookie.com/embed/${video.youtube_id}`} title={video.title} loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/></div><div><span>{formatDate(video.published_at,locale)}</span><h3>{video.title}</h3>{video.description&&<p>{video.description}</p>}</div></article>)}</div>:<div className="clubDarkEmpty">{text.videosEmpty}</div>}</div></section></main>}
function formatDate(value:string|null,locale:Locale){if(!value)return publicText[locale].media.defaultAlbum;return new Intl.DateTimeFormat(dateLocale(locale),{day:"2-digit",month:"long",year:"numeric",timeZone:"Europe/Chisinau"}).format(new Date(`${value}T12:00:00`))}
